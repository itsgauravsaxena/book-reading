// Firebase project config for the bibliophil web prototype.
//
// This is the public "web app" config Firebase issues per project — it is
// NOT a secret (access is enforced by Firestore security rules and Auth,
// not by hiding this object), so it's safe to commit.
//
// Get your own values from: Firebase console → Project settings (gear icon)
// → General tab → "Your apps" → the web app (</>) → SDK setup and configuration.
// See docs/FIREBASE_SETUP.md for the full walkthrough.
export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME"
};
